# REQUIRED in production (app refuses default secrets when NODE_ENV=production)
ADMIN_PASSWORD=change-me-strong-password
SESSION_SECRET=change-me-long-random-string-at-least-32-chars
TELEGRAM_USERNAME=your_telegram

# MongoDB Atlas — https://cloud.mongodb.com
MONGODB_URI=
MONGODB_DB=wanglins

# Optional public site URL for metadata
# NEXT_PUBLIC_SITE_URL=https://example.com
